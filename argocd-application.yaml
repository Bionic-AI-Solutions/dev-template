apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: dev-template
  namespace: argocd
  labels:
    app: dev-template
    environment: production
spec:
  project: default
  source:
    repoURL: https://github.com/Bionic-AI-Solutions/dev-template.git
    targetRevision: main
    path: k8s/overlays/production
  destination:
    server: https://kubernetes.default.svc
    namespace: dev-template
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
    - PrunePropagationPolicy=foreground
    - PruneLast=true
  revisionHistoryLimit: 10
