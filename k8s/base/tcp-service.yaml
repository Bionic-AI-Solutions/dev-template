apiVersion: v1
kind: Service
metadata:
  name: dev-template-ssh-service
  namespace: dev-template
  labels:
    app: dev-template
spec:
  type: ClusterIP
  ports:
  - port: 22
    targetPort: 22
    protocol: TCP
    name: ssh
  selector:
    app: dev-template
