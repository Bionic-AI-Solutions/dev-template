apiVersion: apps/v1
kind: Deployment
metadata:
  name: dev-template
  namespace: dev-template
  labels:
    app: dev-template
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dev-template
  template:
    metadata:
      labels:
        app: dev-template
    spec:
      containers:
      - name: dev-template
        image: docker4zerocool/dev-template:latest
        ports:
        - containerPort: 22
          name: ssh
        - containerPort: 3000
          name: http
        env:
        - name: NODE_ENV
          value: "production"
        - name: APP_NAME
          value: "dev-template"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          tcpSocket:
            port: 22
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          tcpSocket:
            port: 22
          initialDelaySeconds: 5
          periodSeconds: 5
      restartPolicy: Always